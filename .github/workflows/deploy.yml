name: Next.js CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linter
        run: npm run lint --if-present || echo "No lint script found"

      - name: Build application
        run: npm run build
        env:
          NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}

  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to Server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          timeout: 60s
          command_timeout: 15m
          script: |
            # Navigate to project directory
            cd /var/www/electionbd2026-frontend
            
            # Pull latest changes as webadmin
            sudo -u webadmin git fetch origin main
            sudo -u webadmin git reset --hard origin/main
            
            # Install/update dependencies as webadmin
            sudo -u webadmin npm ci --production=false
            
            # Clean old build and rebuild as webadmin to ensure proper permissions
            sudo -u webadmin rm -rf .next
            sudo -u webadmin npm run build
            
            # Reload PM2 process (zero-downtime) as webadmin
            # Use reload for zero-downtime deployment
            sudo -u webadmin pm2 reload electionbd2026-frontend
            
            # Save PM2 configuration as webadmin
            sudo -u webadmin pm2 save
            
            echo "✅ Frontend deployment completed successfully!"
